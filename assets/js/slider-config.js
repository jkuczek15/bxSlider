/*#######################################
        Slider JavaScript Configuration 
#########################################*/

/* Please see https://v4-alpha.getbootstrap.com/components/carousel/#options */

var interval = false;       /* false = prevent autoplay, 1000 = 1 second autoplay */
var keyboard = true;        /* whether or not keyboard input affects slider */
var pause = 'hover';        /* if interval is set, only autoplay when mouse hover occurs */
var ride = false;           /* autoplays the slider after user manually cycles first item */
var wrap = false;            /* whether the carousel should cycle continuously or have hard stops. */